export const HOME_CONTENT_FLAGS = {
  // true: always show playlist recommendations only (test mode)
  forcePlaylistOnlyTestMode: false,
} as const;

export const HOME_CONTENT_TIMING = {
  introDurationMs: 10000,
  introRotationMs: 10000,
  listeningRotationMs: 10000,
} as const;
